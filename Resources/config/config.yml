# Da Api Client
da_api_client:
    api:
        media:
            endpoint_root: %api_client_media_endpoint_root%
            cache_enabled: true

twig:
    form:
        resources:
            - 'TmsMediaClientBundle:Form:fields.html.twig'
